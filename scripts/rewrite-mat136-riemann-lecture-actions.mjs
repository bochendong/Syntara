#!/usr/bin/env node

import fs from 'node:fs';
import path from 'node:path';
import { PrismaClient } from '@prisma/client';
import {
  rebuildActions,
  rebuildSceneContent,
  targetsForScene,
  validateScene,
} from './enhance-mat136-spotlight-focus.mjs';
import { sanitizeMathForSpeech } from './mat136-tts-speech.mjs';

const NOTEBOOK_ID = 'nb-mat136-riemann-integral-week1-20260518135718';
const DRY_RUN = process.argv.includes('--dry-run');

function loadEnvLocal() {
  const envPath = path.resolve(process.cwd(), '.env.local');
  if (!fs.existsSync(envPath)) return;

  for (const line of fs.readFileSync(envPath, 'utf8').split(/\r?\n/)) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith('#')) continue;
    const match = trimmed.match(/^([A-Za-z_][A-Za-z0-9_]*)=(.*)$/);
    if (!match) continue;
    const [, key, rawValue] = match;
    if (process.env[key]) continue;
    process.env[key] = rawValue.replace(/^['"]|['"]$/g, '');
  }
}

const RIEMANN_NARRATION = {
  'MAT 136 · 黎曼积分': [
    [
      '我们先不急着写定义。今天这节课的核心问题很朴素：曲线下面这块弯弯的面积，不能直接用一个长乘宽算，那我们能不能用很多个小矩形把它逼出来。',
      '请先把视线放在左边第一幅图。这里的矩形很少，每个矩形都很宽，所以它只是一个很粗的估计。我们现在要学的不是一下子算准，而是先建立近似的动作。',
    ],
    [
      '中间这幅图把区间切得更细。矩形数量变多以后，你会发现矩形上边缘开始更贴近曲线。这个变化很重要：方法没有变，仍然是矩形，只是分割更细。',
      '这里可以先停一下想一想：如果继续把每一段切得更窄，矩形和曲线之间的空隙会发生什么变化？这就是后面极限思想的入口。',
    ],
    [
      '右边第三幅图继续细分。现在矩形已经像是在沿着曲线走，虽然每一个矩形仍然是直边，但整体形状越来越接近曲线下面的区域。',
      '这就是今天的视觉主线：从粗糙的矩形估计，逐渐走向稳定的面积数。后面出现的所有符号，都是为了把这个过程说清楚。',
    ],
    [
      '最右边这个 A，不是随便放的装饰。它代表我们希望最后得到的那个稳定面积值。',
      '注意，A 不是某一次分割算出来的临时答案，而是当分割不断变细之后，所有合理矩形和共同靠近的那个数。',
    ],
    [
      '底部这条路线把今天的课串起来。先会做 Riemann sums，也就是矩形面积和；再讨论什么时候这些和会稳定；最后把稳定下来的面积和下一节的 definite integral 接上。',
      '所以今天不要把重点放在背符号。你要不断问自己：这一页是在说明矩形的哪一步，是切区间、选高度、求和，还是取极限。',
    ],
    [
      '这一页的收束句是：黎曼积分的想法，就是用越来越细的矩形，去逼近曲线下面的面积。',
      '下一页我们把这个想法拆成四个具体动作。拆开之后，后面的公式就不会像突然冒出来的一串符号。',
    ],
  ],
  '黎曼积分：从矩形到极限': [
    [
      '这一页是整节课的地图。请先看四个编号，而不是急着看右上角的求和公式。四个编号分别是面积、分割、采样、极限。',
      '这四件事合起来，才是黎曼积分的完整故事。如果少掉其中任何一步，后面的定义都会变得很空。',
    ],
    [
      '第一格讲的是面积。曲线下面积本身比较复杂，所以我们的策略是把它拆成许多小块。每一小块先用矩形近似，再把它们加起来。',
      '这里最需要抓住的是“局部变简单”。整块面积不好算，但每一小段可以暂时当作一个矩形。',
    ],
    [
      '第二格是分割区间。我们在 a 到 b 之间插入一串点，把原来的大区间切成很多小区间。',
      '这些小区间未来会变成矩形的底边。也就是说，分割决定的是每个矩形有多宽。',
    ],
    [
      '第三格是采样点。只有宽度还不够，矩形还需要高度。每个小区间里选一个点，用函数在这个点的高度代表这一整小段。',
      '先停一下：为什么不能只说“这一段的高度”？因为曲线在这一段里可能一直变化，所以我们必须说明到底取哪一个点的高度。',
    ],
    [
      '第四格是极限。一次矩形和只是一个近似，不是最终答案。真正的积分思想，是观察当分割越来越细时，这些近似值会不会稳定。',
      '这里的稳定不是看图感觉差不多，而是要求所有合理的分割和采样，最后都逼近同一个数。',
    ],
    [
      '底部的问题就是今天的总问题：矩形和怎样逼近曲线下面积。接下来每一页都会回答其中一个小步骤。',
      '下一页先从最直观的地方开始：为什么面积本来就适合用“累加”的方式理解。',
    ],
  ],
  '面积为什么是累积量？': [
    [
      '这一页先把“面积是累积量”讲慢一点。左边是最简单的情况：速度恒定，所以图像是一条水平线。',
      '如果速度一直是 v，从 a 到 b 经过的时间长度就是 b 减 a。距离等于速度乘时间，这在图像上就是一个矩形面积。',
    ],
    [
      '中间这行式子并不是新公式，它只是把刚才的生活直觉写出来。高度是 v，宽度是 delta t，高度乘宽度就是这一小段累积出来的量。',
      '所以当函数值表示速度、流量、变化率这类东西时，图像下面的面积往往就表示某种总量。',
    ],
    [
      '右边开始进入更真实的情形：高度会变化。速度不是一直固定，曲线就不再对应一个单独的矩形。',
      '这时我们不能直接用一个高度乘整个宽度，因为选哪个高度都会不公平。函数在不同位置的高度并不一样。',
    ],
    [
      '解决办法是局部近似。把时间轴切成很多小段，在每一小段里，函数变化没有那么剧烈，于是可以先用一个小矩形代表这一段。',
      '请注意“先用”两个字。矩形不是说真实图形就是矩形，而是我们先用它来做可计算的近似。',
    ],
    [
      '底部这句话是本页的核心思想：把复杂面积拆成许多简单矩形。听起来简单，但这正是后面求和公式的来源。',
      '下一页我们正式做第一件事：要拆成小矩形，先得把区间切开。',
    ],
  ],
  '第一步：分割区间 P': [
    [
      '现在进入第一步：分割区间。先看上方这条横轴，从 a 到 b，中间有一串点。',
      '这些点不是装饰，它们规定了我们在哪里切区间。每两个相邻点之间，就是一个未来的小矩形底边。',
    ],
    [
      '把这一串点合在一起，就叫一个 partition，中文可以说分割。它像是一份切割方案，告诉我们从哪里切到哪里。',
      '所以 P 不是一个单独的点，而是一整组点：x_0、x_1，一直到 x_n。',
    ],
    [
      '现在看橙色标出的这一段。它是第 i 个小区间，从 x_{i-1} 到 x_i。',
      '这一段的宽度就是 delta x_i，也就是右端点减左端点。后面每个小矩形的面积，都会用到这个宽度。',
    ],
    [
      '左下角的分割表示把符号写严谨：a 等于 x_0，小于 x_1，一直小于 x_n，最后等于 b。',
      '这一长串不等式只是告诉我们：这些切点按从左到右的顺序排列，覆盖了整个区间。',
    ],
    [
      '右下角讲等分的特殊情况。如果题目说把区间等分成 n 段，那么每段宽度相同，都是 b 减 a 除以 n。',
      '但要记住，黎曼积分并不只允许等分。不等宽的分割也可以，只是等分题最容易计算。',
    ],
    [
      '本页最后留下一个直觉：分割越细，每个矩形负责的范围越小，它就越有机会贴近曲线。',
      '下一页我们解决另一个问题：底边有了，矩形高度到底从哪里来。',
    ],
  ],
  '第二步：选择采样点 c_i': [
    [
      '上一页我们已经切好了底边。现在要给每个矩形定高度。高度来自哪里？来自小区间里的一个采样点 c_i。',
      '采样点的意思是：在这一小段里选一个代表位置，用函数在这个位置的值来当矩形高度。',
    ],
    [
      '左边是左端点规则。每个小区间都选最左边的点，所以 c_i 等于 x_{i-1}。',
      '如果曲线在上升，左端点往往会拿到较低的高度；如果曲线在下降，左端点又可能拿到较高的高度。这个伏笔后面会用来判断高估低估。',
    ],
    [
      '中间是右端点规则。每个小区间都选最右边的点，也就是 c_i 等于 x_i。',
      '注意，左端点和右端点的底边完全一样，区别只在高度取自哪里。',
    ],
    [
      '右边是中点规则。它选小区间正中间的位置，所以 c_i 等于左右端点的平均值。',
      '中点规则提醒我们，采样点不一定只能是端点。只要落在自己的小区间里，它就是合法选择。',
    ],
    [
      '中间这条公式把矩形面积说清楚：面积等于 f(c_i) 乘 delta x_i。',
      '请把这句话拆开记：f(c_i) 是高度，delta x_i 是宽度。一个来自采样，一个来自分割。',
    ],
    [
      '所以第二步的结论是：采样点决定矩形的高度。',
      '下一页我们会把所有这些小矩形面积加起来，得到一个真正可以计算的近似值。',
    ],
  ],
  '第三步：把矩形面积加起来': [
    [
      '现在宽度和高度都有了。第三步非常自然：把每个小矩形的面积全部加起来。',
      '先看左边图像，不要先看公式。每一个蓝色小矩形都是曲线下方某一小段面积的近似。',
    ],
    [
      '橙色高亮的这一块代表第 i 个矩形。它的高度是 f(c_i)，宽度是 delta x_i。',
      '这一步如果看懂了，右边公式就只是这句话的符号版本。',
    ],
    [
      '右上角先写单个矩形面积：A_i 等于 f(c_i) delta x_i。',
      '这里的 i 表示任意一个小区间。不是只算某一块，而是给每一块都规定同样的计算方式。',
    ],
    [
      '右下角出现求和符号。sigma 看起来复杂，但它的意思只是：从第一块矩形开始，一块一块加到第 n 块。',
      '可以在心里读成：第一块面积，加第二块面积，加第三块面积，一直加下去。',
    ],
    [
      '这个总和 S(P,c) 就叫 Riemann sum。P 表示怎么分割，c 表示每段选了什么采样点。',
      '所以同一个函数，因为 P 和 c 不同，可能会得到不同的黎曼和。',
    ],
    [
      '本页的收束是：Riemann sum 是一个近似面积的数，还不是最终面积。',
      '下一页进入具体例题，用左端点规则算一遍，让这个公式真正落地。',
    ],
  ],
  '例题 1：左黎曼和估计 √x': [
    [
      '第一道计算题，我们先不动笔算，先读题。题目给了四个信息：区间是三十二到四十，分成四段，使用左端点，函数是根号 x。',
      '读题这一步很重要，因为黎曼和题目的错误，常常不是算术错，而是采样点选错。',
    ],
    [
      '看左边图像。区间长度是四十减三十二，也就是八。分成四段，所以每段宽度 delta x 等于二。',
      '先算宽度，是因为后面每个小矩形面积都要乘这个二。',
    ],
    [
      '现在看橙色点。因为是左端点规则，四个采样点不是三十四、三十六、三十八、四十，而是三十二、三十四、三十六、三十八。',
      '请特别注意四十。它是整个区间的最右边界，但不是任何一个小区间的左端点采样值。',
    ],
    [
      '右侧步骤把刚才的观察写成式子：L_4 等于二乘以根号三十二、根号三十四、根号三十六、根号三十八的和。',
      '这里的二是宽度，括号里四项是四个矩形的高度。',
    ],
    [
      '计算后约等于四十七点三零四。这个数表示四个左端点矩形给出的面积估计。',
      '因为根号 x 在这个区间上是递增的，左端点高度偏低，所以这个估计通常会比真实面积小一点。',
    ],
    [
      '本题的迁移方法是：看到 left sum，先画或列出每段左边界，再把这些点代入函数，最后乘宽度。',
      '下一页我们把公式拿掉，只给表格。你会看到，只要知道采样点高度，仍然可以算黎曼和。',
    ],
  ],
  '例题 2：只有表格，也能算右黎曼和': [
    [
      '这一页故意不给函数公式，只给表格。先意识到：黎曼和的本质是取高度乘宽度，不一定非要知道完整函数表达式。',
      '如果表格能告诉我们采样点的高度，就已经够用了。',
    ],
    [
      '先读表格。x 的位置是零、二、四、六、八，对应的 f(x) 是三、五、四、七、六。',
      '这些数字就是我们能使用的高度信息。不要试图从这五个点猜一条公式，那不是本题需要做的事。',
    ],
    [
      '题目要求右黎曼和，所以每个小区间取右边的表格值。',
      '区间零到八被分成四段：零到二、二到四、四到六、六到八。它们的右端点分别是二、四、六、八。',
    ],
    [
      '每段宽度是二。这一步和上一题一样，宽度来自区间长度除以段数。',
      '现在把右端点对应的高度取出来：f(2) 是五，f(4) 是四，f(6) 是七，f(8) 是六。',
    ],
    [
      '所以 R_4 等于二乘以五加四加七加六，结果是四十四。',
      '这里的四十四仍然是面积近似，不是精确面积。它依赖于我们使用右端点作为采样规则。',
    ],
    [
      '本页总结成做题步骤：先确定分割和宽度，再确定采样规则，再查表取高度，最后相加。',
      '下一页我们把左端点、右端点、中点放在一起比较，看看采样规则到底改变了什么。',
    ],
  ],
  三种常见采样规则: [
    [
      '现在把三种采样规则并排放在一起。请先看共同点：三张图用的是同一个区间和同一种分割。',
      '也就是说，每个矩形的底边位置是一样的，真正改变的是矩形高度从哪里取。',
    ],
    [
      '左边是左端点和。每段都用左边界的函数值当高度，所以采样点全部贴在小区间左侧。',
      '以后看到 L_n，先在脑子里想“每段左边的高度”。这个图像比死记符号更稳。',
    ],
    [
      '中间是右端点和。采样点换到每段右边界，所以高度会跟左端点和不同。',
      '如果函数在上升，右端点高度会更大；如果函数在下降，右端点高度会更小。',
    ],
    [
      '右边是中点和。采样点放在小区间中间，它常常比单纯左端或右端更平衡。',
      '但我们暂时不说它一定更准确，因为准确性还要看函数形状。现在只要理解采样位置不同。',
    ],
    [
      '底部这句话是本页重点：同一个分割，不同 c_i，会得到不同近似。',
      '下一页我们继续追问：这些不同近似，有没有办法判断谁偏高、谁偏低。',
    ],
  ],
  '高估 / 低估：先看单调性': [
    [
      '判断高估低估，第一眼不要看公式，先看函数单调性。函数是递增还是递减，会决定左端点和右端点的方向。',
      '我们慢慢看四张图，每张图都只问一件事：矩形顶边是在曲线下面，还是盖过曲线。',
    ],
    [
      '左上角是递增函数配左端点。每段左边的高度，是这一段里比较低的高度。',
      '所以左端点矩形大多压在曲线下面，面积近似偏小。这就是递增函数里 left sum 低估的原因。',
    ],
    [
      '右上角还是递增函数，但采样点变成右端点。每段右边的高度比较高。',
      '矩形顶边会盖过曲线，所以 right sum 偏大。这里和左端点刚好相反。',
    ],
    [
      '左下角换成递减函数。现在每段左端点反而是比较高的位置。',
      '所以左端点矩形会盖过曲线，left sum 变成高估。注意不是左端点永远低估，它要看单调性。',
    ],
    [
      '右下角是递减函数配右端点。右端点是每段较低的位置，所以矩形落在曲线下面。',
      '因此 right sum 变成低估。四张图合在一起，就是底部的不等式结论。',
    ],
    [
      '本页的判断方法是：先看函数增减，再看采样点在每段的高处还是低处。',
      '下一页我们不再只比较左右端点，而是让分割越来越细，看看误差能不能被压小。',
    ],
  ],
  '让分割变细：mesh 趋近 0': [
    [
      '上一页的高估低估，都来自矩形和曲线之间的空隙。现在我们做一个自然动作：把区间切得更细。',
      '先看左边粗分割。矩形宽，每块负责的范围大，所以曲线变化被粗暴地用一个高度代表，误差自然明显。',
    ],
    [
      '中间的 refine 表示细化分割。它不是换一种算法，而是在原来思想上多切几刀。',
      '可以想象：每一段变短以后，在这一小段内函数高度变化就没那么大。',
    ],
    [
      '右边是细分割。矩形变窄以后，顶部和曲线之间的差距变小，面积近似看起来更可靠。',
      '但是数学上不能只说“看起来”。我们需要一个量来描述分割到底有多细。',
    ],
    [
      '定义框里的 mesh，就是所有小区间宽度里的最大值。',
      '为什么看最大值？因为只要还有一段很宽，那一段就可能制造很大误差。平均宽度小还不够，最粗的一段也要变小。',
    ],
    [
      '所以 mesh 趋近零，意思是整个分割里最宽的小区间也越来越窄。',
      '这比简单说 n 越来越大更一般，因为它允许每段宽度不完全相等。',
    ],
    [
      '底部问题是进入定义的关键：如果所有合法采样得到的和，都随着 mesh 趋近零而走向同一个数，会发生什么？',
      '下一页我们就把这个“同一个数”正式变成可黎曼积分的定义。',
    ],
  ],
  '什么时候叫“可黎曼积分”？': [
    [
      '这一页是整节课最重要的定义页。先看左边：任意采样点 c_i，意思是每段里怎么选点都可以，只要点落在自己的小区间里。',
      '所以可黎曼积分不是只检查左端点或右端点，而是要对所有合理采样都稳定。',
    ],
    [
      '中间三张小图分别表示左端点和、右端点和，以及一般采样点得到的和。',
      '一开始它们可能不同，因为矩形高度不同。但随着分割越来越细，这些差异应该被压小。',
    ],
    [
      '现在看箭头。三个不同来源的 Riemann sums，都被拉向同一个 A。',
      '这个“同一个”非常关键。如果左端点逼近一个数，右端点逼近另一个数，那我们就没有稳定的面积定义。',
    ],
    [
      '右侧框里强调 A 不依赖分割，也不依赖采样点。换句话说，不管你怎么切、怎么选高度，只要 mesh 趋近零，最后都走向 A。',
      '这就是为什么 A 可以被看成曲线下面真正稳定的面积数，而不是某种计算规则的偶然产物。',
    ],
    [
      '底部定义可以读成一句人话：如果矩形面积和在分割无限变细时稳定到同一个数，我们就说函数可黎曼积分。',
      '这里的“可积”不是说我们已经会算，而是说这个面积对象本身是稳定存在的。',
    ],
    [
      '最后一句把下一节打开：这个稳定的 A，以后会被记作 definite integral，也就是定积分。',
      '所以今天这节课的目的，是让定积分符号背后的面积意义变清楚。',
    ],
  ],
  '本节课总结：黎曼积分的流程': [
    [
      '最后一页不要匆匆念完。我们要把整节课重新串成一条流程，把每个符号从哪里来讲清楚。',
      '上方五张卡片就是流程骨架：分割区间，选采样点，做矩形，求和，让 mesh 趋近零。',
    ],
    [
      '第一步分割区间，是决定每个矩形的宽度。第二步选采样点，是决定每个矩形的高度。',
      '这两个动作合起来，才产生单个矩形面积 f(c_i) delta x_i。',
    ],
    [
      '第三步把矩形放到曲线下面，第四步把所有矩形面积加起来，得到 Riemann sum。',
      '这个和依赖于分割 P 和采样点 c，所以它一开始只是一个近似。',
    ],
    [
      '第五步让 mesh 趋近零。也就是让最宽的小区间也越来越窄，逼迫所有矩形都越来越局部。',
      '如果这时所有合法 Riemann sums 都趋向同一个 A，我们才说函数可黎曼积分。',
    ],
    [
      '中间的公式现在可以重新读一遍：每一项都是一个高度乘一个宽度，sigma 只是把所有小矩形加起来。',
      '公式不是重点本身，公式是对整套几何动作的压缩。',
    ],
    [
      '右侧图像再次提醒从粗到细的过程。矩形越窄，曲线和矩形之间的误差越小，近似值越有机会稳定。',
      '下一节我们会把这个稳定的 A 写成定积分符号，并学习怎样不用真的画无限多个矩形也能计算它。',
    ],
  ],
};

function buildSpeechActions(scene, groups) {
  const actions = [];
  let speechIndex = 1;

  groups.forEach((lines) => {
    lines.forEach((text) => {
      actions.push({
        id: `${scene.id}-speech-riemann-${String(speechIndex).padStart(2, '0')}`,
        type: 'speech',
        title: `讲解：${scene.title} ${speechIndex}`,
        text: sanitizeMathForSpeech(text),
      });
      speechIndex += 1;
    });
  });

  return actions;
}

function validateSceneCoverage(scenes) {
  const sceneTitles = new Set(scenes.map((scene) => scene.title));
  const missingInDb = Object.keys(RIEMANN_NARRATION).filter((title) => !sceneTitles.has(title));
  if (missingInDb.length > 0) {
    throw new Error(`Narration references missing scenes: ${missingInDb.join(', ')}`);
  }

  const missingNarration = scenes.filter((scene) => !RIEMANN_NARRATION[scene.title]);
  if (missingNarration.length > 0) {
    throw new Error(
      `Scenes without narration: ${missingNarration.map((scene) => scene.title).join(', ')}`,
    );
  }
}

async function main() {
  loadEnvLocal();
  const prisma = new PrismaClient();

  try {
    const notebook = await prisma.notebook.findUnique({
      where: { id: NOTEBOOK_ID },
      include: { scenes: { orderBy: { order: 'asc' } } },
    });
    if (!notebook) throw new Error(`Notebook not found: ${NOTEBOOK_ID}`);

    validateSceneCoverage(notebook.scenes);

    let speechTotal = 0;
    let sceneTotal = 0;

    for (const scene of notebook.scenes) {
      const groups = RIEMANN_NARRATION[scene.title];
      const speechActions = buildSpeechActions(scene, groups);
      const sceneForAlignment = { ...scene, actions: speechActions };
      const targets = targetsForScene(NOTEBOOK_ID, sceneForAlignment);
      const content = rebuildSceneContent(sceneForAlignment, targets);
      const actions = rebuildActions(sceneForAlignment, targets);
      validateScene(sceneForAlignment, content, actions);

      speechTotal += actions.filter((action) => action.type === 'speech').length;
      sceneTotal += 1;

      if (!DRY_RUN) {
        await prisma.scene.update({
          where: { id: scene.id },
          data: { content, actions },
        });
      }
    }

    if (!DRY_RUN) {
      await prisma.notebook.update({
        where: { id: NOTEBOOK_ID },
        data: { updatedAt: new Date() },
      });
    }

    console.log(
      `${DRY_RUN ? 'Would update' : 'Updated'} 01 - 黎曼积分: ${sceneTotal} scenes, ${speechTotal} speech segments`,
    );
  } finally {
    await prisma.$disconnect();
  }
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
